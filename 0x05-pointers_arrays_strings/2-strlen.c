#include "main.h"

/**
 * _strlen - lenght of a string
 * @s: strlen
 * Return: length of string
 */
int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0'; index++)
	return (index);
}
